require "spec_helper"

describe Gitterlog do
  it "has a version number" do
    expect(Gitterlog::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
